services:
  - type: web
    name: kamala-trader-backend
    env: node
    plan: free
    buildCommand: cd server && npm install
    startCommand: cd server && npm run start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3001
